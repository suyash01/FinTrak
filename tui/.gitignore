# Coverage profiles written by `make test-tui-cover-check` (same rules as
# backend/.gitignore, which owns the coverage.out convention).
*.out
coverage.*

# Binary produced by the Dockerfile's build (`go build -o fintrak-tui .`).
/fintrak-tui
/fintrak-tui.exe
