[project]
name = "statement-parser"
version = "0.1.0-alpha.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Suyash Mittal", email = "suyash8514@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pdfplumber>=0.11.10",
    "pypdf>=6.19.0",
    "gunicorn>=26.2.0",
]

[project.scripts]
statement-parser = "statement_parser:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "coverage>=7.16.1",
]
