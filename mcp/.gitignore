# Coverage profiles written by `make test-mcp-cover-check` (same rules as
# backend/.gitignore, which owns the coverage.out convention).
*.out
coverage.*

# Binary produced by `go build -o fintrak-mcp .`
/fintrak-mcp
/fintrak-mcp.exe
